# 导向标识生成器

一个用于创建城市轨道交通导向标识牌的交互式编辑工具。

## Language

**标识牌 (Sign)**:
一块由多行排列的元素组成的导向信息展示板，有固定的宽度和各行高度，最终可导出为 SVG 或 PNG。
_Avoid_: 画布、指示牌、导视牌

**行 (Row)**:
标识牌中的一条水平带，包含一组水平排列的元素。各行等宽。
_Avoid_: 层、条、line

**元素 (Element)**:
放置在标识牌行内的一个可视化组件，如箭头、线路编号、出入口标识等。元素不自设高度和宽度——高度继承所在行的行高，宽度由内容动态计算。可编辑的属性包括内容、颜色、内边距等。
_Avoid_: 组件、控件、widget

**标识牌宽度 (Sign Width)**:
整块标识牌的统一宽度，所有行等宽。支持两种模式：

- **固定宽度**：用户手动设定像素宽度
- **动态宽度**：宽度自动由最宽的一行决定（含元素边距）
_Avoid_: 画布宽度

**锁定宽高比 (Lock Aspect Ratio)**:
启用后，修改行高时标识牌宽度等比缩放，或反之。确保放大缩小不改变标识牌的视觉比例。
_Avoid_: 等比例缩放

**行高 (Row Height)**:
标识牌中每一行的高度（默认 256px），各行统一。行内所有元素自动适配此高度，边距等子属性按行高比例计算。
_Avoid_: size、元素高度

**编辑区 (Edit Area)**:
标识牌的实时 SVG 预览区，所见即所得，支持点击选中元素。
_Avoid_: 画布、预览窗

**元素选择区 (Element Palette)**:
左侧分类陈列可添加元素的区域，支持拖放到编辑区。
_Avoid_: 元件库、工具箱、侧边栏

**预设 (Preset)**:
一行完整元素的快照，包含所有元素的类型、内容、颜色、边距等全部属性。用户可将任意行保存为预设（存于 localStorage），拖入预设将替换目标行的全部元素。
_Avoid_: 模板、组合、combo

**图标 (Icon)**:
服务设施（电梯、洗手间、楼梯等）与方向 pictogram 元素，库见 `web-app/js/icons.js`（由 `web-app/gen-icons.mjs` 从外部 SVG 生成，勿手工编辑）。图形为内嵌 SVG path，渲染时 `fill="black"` 替换为元素颜色，白色部分保留（色底白图形的 tile 风格）。不依赖字体，导出自包含。
_Avoid_: 图片、emoji、pictogram

**元素对齐 (Element Align)**:
固定宽度模式下，元素在标识牌内的水平对齐方式：贴左、居中、贴右。三种对齐如同三条独立排列的通道，各自计算通道内元素的排列；不同通道的元素可能重叠，属设计之内，由用户调大牌宽解决。动态宽度模式下不支持，所有元素左起排列。与文字对齐（双语文本内部的中英文对齐）相互独立，属性名为 `elementAlign`。
_Avoid_: 文字对齐、水平对齐

**灰框 (Sign Frame)**:
标识牌的可选边框（`frameWidth`，单位 px，0 为关闭，颜色固定灰 #808080）。从牌面宽度边界向内延伸指定宽度的直角边框，由四条矩形拼成；每一行都有自己的灰框，以此模拟真实场景中每块牌面独立的边框。灰框渲染在所有元素**之上**（遮挡越界的元素内容，`pointer-events` 关闭不拦截点击），随 SVG/PNG 一同导出。
_Avoid_: 边框、描边、外框

**内容对齐 (Content Align)**:
数字线路、文字线路、出口元素内部的左/右镜像排版（属性名 `align`，仅 left|right 两档，默认 left），用于贴合现实中的右置导视牌。右对齐时：色块从元素左侧移至右缘，双语标签（号线 Line / 线 Loop Line / 出口 Exit）在标签盒内右对齐，出口编号移到「出口」右侧（编号基线不变）；数字线路仅渲染第 1 条线路（其余保留，切回左对齐恢复）。与元素对齐相互独立。
_Avoid_: 元素对齐、镜像模式

**线路名下沉 (Name Sink)**:
文本线路元素的排版开关（属性名 `nameSink`，默认开启）。开启时线路名以大字显示并与小字「线」组合成完整线路名，即"下沉"排版；关闭时线路名与「线」字同字号（同一双语标签排版）忠实显示输入的完整文本，不再拼接「线」后缀，英文名仍显示在其下方，色条不变。
_Avoid_: 大字模式、平铺模式

**城市配色 (Palette City)**:
选色板线路配色的城市（上海/重庆/成都），属编辑器偏好（`paletteCity`），不随标识牌数据保存。切换后所有颜色选择器的色板同步重建，「输入线路号自动匹配线路色」也按当前城市的线路表填充。多条线路共用的颜色在色板中合并为一个色块（如重庆的潘通 2935C 蓝）；灰/黑/白为通用色，不随城市变化。
_Avoid_: 主题、配色方案
